steps:
  # 1. בניית האימג'
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build Backend'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/${PROJECT_ID}/shay-registry/shay-backend:$SHORT_SHA', 'backend/']

  # 2. דחיפה ל-Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push Backend'
    args: ['push', 'us-central1-docker.pkg.dev/${PROJECT_ID}/shay-registry/shay-backend:$SHORT_SHA']

  # 3. פריסה ל-Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy Backend'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'shay-backend'
      - '--image'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/shay-registry/shay-backend:$SHORT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      # שים לב: לבקנד אין allow-unauthenticated ברירת מחדל בקוד הזה, 
      # אלא אם כן נרצה להוסיף. כרגע זה מאובטח.

substitutions:
  _REGION: "us-central1"
