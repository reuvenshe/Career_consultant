# Copy this file to backend/.env.docker and fill the real values before running
# Do NOT commit the real backend/.env.docker to source control.

# Database settings for docker-compose
DB_HOST=mysql
DB_USER=root
DB_PASSWORD=example_root_password
DB_NAME=Shay_Project

# OpenAI API key (set your real key here)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE

# Server port (optional)
PORT=3000
