
options:
  # הפקודה הזו אומרת לגוגל: "שלח את הלוגים למערכת היומן הרגילה של GCP, 
  # אל תנסה להשתמש ב-GCS Bucket."
  logging: CLOUD_LOGGING_ONLY
steps:
  # 1. בניית האימג' (Docker Build)
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build Frontend'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/${PROJECT_ID}/shay-registry/shay-frontend:$SHORT_SHA', 'frontend/']

  # 2. דחיפה ל-Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push Frontend'
    args: ['push', 'us-central1-docker.pkg.dev/${PROJECT_ID}/shay-registry/shay-frontend:$SHORT_SHA']

  # 3. פריסה ל-Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy Frontend'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'shay-frontend'
      - '--image'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/shay-registry/shay-frontend:$SHORT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'

substitutions:
  _REGION: "us-central1"